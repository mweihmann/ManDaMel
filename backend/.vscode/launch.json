{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Xdebug Backend",
            "type": "php",
            "request": "launch",
            "port": 9003,
            "pathMappings": {
                "/var/www/html": "${workspaceFolder}",
                "/var/www/html/public": "${workspaceFolder}/public",
                // "/var/www": "${workspaceFolder}"
            }
        }
    ]
}
  